import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.octolabs.artisanmu",
  appName: "Artizan Moris",
  webDir: "out",
  android: {
    allowMixedContent: false,
  },
};

export default config;
